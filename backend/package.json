{
  "name": "sgfman-backend",
  "version": "1.0.0",
  "description": "SGFMAN backend with Express, SQLite, Nodemailer, Cron",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "postinstall": "node -e \"if (process.env.RENDER) { process.exit(0) } else { process.exit(0) }\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "nodemailer": "^6.9.3",
    "sqlite3": "^5.1.6"
  }
}
